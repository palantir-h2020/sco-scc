quarkus:
  datasource:
    devservices:
      enabled: true

  mongodb:
    devservices:
      enabled: true
      image-name: mongo:latest
      port: 27017

  rest-client:
    sco-so-api:
      url: http://localhost:50101/pkg
